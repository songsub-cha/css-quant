# packages/api-types

`apps/api`의 OpenAPI 스펙에서 자동 생성되는 TypeScript 타입 패키지 (SoT B5.8).

백엔드 응답 스키마가 바뀌면 이 패키지를 재생성한다. **수동 편집 금지** — 생성 파이프라인이 유일한 소스다.

## 현재 상태

생성 파이프라인은 아직 구성되지 않았다. `apps/api`의 OpenAPI 엔드포인트가 갖춰진 후 후속 이슈에서 연결한다.
