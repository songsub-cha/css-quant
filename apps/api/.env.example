# Copy to .env and adjust for local use. Never commit the real .env (see
# repo .gitignore).

# Postgres connection string. Any of postgres://, postgresql://,
# postgresql+asyncpg://, postgresql+psycopg2:// are accepted and
# auto-rewritten to the psycopg 3 driver at load time (SoT B5.1 / ADR 0005 —
# asyncpg is banned after a real SSL incompatibility with managed Postgres).
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/quantpilot

# Redis connection string (Arq worker + cache, SoT B5.4). This default targets
# a locally-run redis (e.g. `uv run uvicorn`/`uv run arq` outside Docker). Inside
# docker-compose.dev.yml this is overridden to redis://redis:6379/0 — containers
# reach Redis by service name, not localhost.
REDIS_URL=redis://localhost:6379/0

# Cookie Secure flag (SoT B4.6 / D2) — its own env var, deliberately not
# derived from ENVIRONMENT (predecessor-repo trap: "production" + HTTP-only
# IP access locked the owner out of login). Settings.cookie_secure has NO
# Python-level default, so this must be set explicitly for every deployment:
#   true  = Tailscale HTTPS (SoT D3 primary access path)
#   false = LAN HTTP fallback (PWA/web push/Google OAuth unavailable)
# If you have a pre-existing local .env from before this line was added,
# you must add it by hand — the app (and `uv run pytest`) will fail to boot
# without it.
COOKIE_SECURE=false

# Signup gate (SoT D2). Stays false except for the one-time owner bootstrap
# (flip true, create the owner account, flip back to false).
SIGNUP_ENABLED=false

# JWT signing key (SoT D2/B4.6). No default, and an empty value is rejected
# (min_length=32) — the app will not boot with a blank or missing key.
# Generate one with: openssl rand -hex 32
SECRET_KEY=

# Data source adapter swap (SoT B3). "fake" (default) never touches the
# network — the whole app runs with zero API keys. "krx" selects the real
# pykrx-backed adapters (PykrxDataSource/PykrxPriceDataSource), with a KRX
# 정보데이터시스템 fallback for ticker master and a FinanceDataReader fallback
# for daily OHLCV (SoT C1/C2).
DATA_SOURCE=fake

# DART OpenAPI key (SoT C1/D2), used only by the real financial-statement
# adapter (DartFinancialStatementDataSource) when DATA_SOURCE=krx. Optional —
# "fake" mode needs no key at all. If DATA_SOURCE=krx and this is left blank,
# the worker fails to boot rather than silently degrading (fail-closed).
# Issue at https://opendart.fss.or.kr/ to get one.
DART_API_KEY=

# Universe filter thresholds (SoT A6.1) — src.workers.universe_filter reads
# these via config.Settings; never hardcoded in engine/worker code.
UNIVERSE_MARKET_CAP_MIN=300000000000
UNIVERSE_AVG_TRADING_VALUE_MIN=1000000000
UNIVERSE_MIN_LISTED_DAYS=60

# Regime-specific factor weights (SoT A6.2) — env-var, not a DB settings
# table (same pattern as the universe thresholds above). Each regime's five
# weights must sum to 1.0 (src.domain.ai_score.FactorWeights validates
# this). src.workers.score_calculation is the only caller.
AI_SCORE_WEIGHT_NORMAL_MOMENTUM=0.35
AI_SCORE_WEIGHT_NORMAL_QUALITY=0.25
AI_SCORE_WEIGHT_NORMAL_VALUE=0.20
AI_SCORE_WEIGHT_NORMAL_LIQUIDITY=0.10
AI_SCORE_WEIGHT_NORMAL_RISK=0.10
AI_SCORE_WEIGHT_DEFENSIVE_MOMENTUM=0.15
AI_SCORE_WEIGHT_DEFENSIVE_QUALITY=0.35
AI_SCORE_WEIGHT_DEFENSIVE_VALUE=0.20
AI_SCORE_WEIGHT_DEFENSIVE_LIQUIDITY=0.10
AI_SCORE_WEIGHT_DEFENSIVE_RISK=0.20

# 5개 팩터(Momentum/Quality/Value/Liquidity/Risk) 중 이 개수 이상이 전부
# 결측이면 해당 종목의 점수 계산을 보류한다 (SoT A6.1 3).
AI_SCORE_MISSING_FACTOR_HOLD_THRESHOLD=3
